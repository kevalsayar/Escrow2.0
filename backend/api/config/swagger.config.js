/**
 * Swagger configurations.
 */

const swaggerOptions = {
  swaggerOptions: {
    filter: "",
    persistAuthorization: true,
    docExpansion: "none",
  },
  customSiteTitle: "Escrow Backend Swagger",
};

module.exports = { swaggerOptions };
